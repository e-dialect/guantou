# Core Django
django==5.0.3
django-cors-headers~=4.3.1
django-simpleui==2023.3.1
djangorestframework~=3.15.2

django-notifications-hq==1.8.3

# Utilities
demjson3~=3.0.6
pyjwt~=2.4.0
django-environ==0.11.2

# Data Processing
xlrd~=2.0.1
numpy>=2.5.0,<3.0.0  # Python 3.12+ compatible, avoid numpy 2.x for compatibility
requests
requests_toolbelt==0.10.1

# Audio Processing
pydub==0.25.1
audioop-lts; python_version >= "3.13"
# Note: ffmpeg and ffprobe are system utilities, not Python packages
# Install via system package manager: apt-get install ffmpeg (Linux) or brew install ffmpeg (macOS)
# The obsolete Python packages 'ffmpeg==1.4' and 'ffprobe==0.5' have been removed

# Cloud Storage
cos-python-sdk-v5

# Development Tools
setuptools<81
black==24.3.0
